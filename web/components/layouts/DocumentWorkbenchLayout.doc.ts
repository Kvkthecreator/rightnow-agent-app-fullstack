/**
 * Layout for editing a single document inside a basket.
 *
 * Used only in: `/baskets/[id]/docs/[did]/work`
 *
 * Features:
 * - Inline narrative editor
 * - Context-aware right panel
 * - Supports blocks, raw dumps, and context items scoped to a single document
 */
