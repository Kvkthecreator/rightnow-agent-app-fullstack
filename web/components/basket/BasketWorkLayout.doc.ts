/**
 * Layout for basket work area showing dashboard and other tabs.
 *
 * Used only in: `/baskets/[id]/dashboard`
 *
 * Features:
 * - Organized sections for documents, blocks, context items, dumps
 * - Clean UI scaffolding for empty vs populated states
 * - Reuses existing "Create" logic from left-nav to ensure consistency
 * - This layout becomes the core workspace when basket has no documents
 */
