'use client';
export default function BlocksCenter({ basketId }: { basketId: string }) {
  return (
    <div className="p-4 text-sm text-muted-foreground">
      Blocks view for basket {basketId} (TODO)
    </div>
  );
}
