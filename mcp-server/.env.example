# YARNNN Backend API URL
BACKEND_URL=http://localhost:10000

# Node Environment
NODE_ENV=development

# MCP Transport Mode
# - stdio: For local Claude Desktop integration (default)
# - http: For cloud deployment with Server-Sent Events
MCP_TRANSPORT=stdio

# HTTP Server Port (only used when MCP_TRANSPORT=http)
PORT=3000

# Optional: Log level
LOG_LEVEL=info
